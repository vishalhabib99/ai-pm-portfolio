services:
  - type: web
    name: acrs-demo
    env: python
    plan: free
    rootDir: demo
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
